type Query {
    _empty: String
}

type Mutation {
    _empty: String
}

type InputError {
    key: String!

    message: String!
}

type Node {
    id: ID!

    dateCreated: DateTime!

    dateUpdated: DateTime!
}

scalar DateTime
