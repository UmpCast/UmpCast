query GetUserOrganizationList {
    me {
        id
        organizationPermitList {
            organization {
                id
                title
                pictureUrl
            }
            permissionList
        }
    }
}
