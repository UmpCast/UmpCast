extend type User {
    organizationPermitList: [UserOrganizationPermit!]
}

type UserOrganizationPermit {
    user: User!
    organization: Organization!
    permissionList: [UserOrganizationPermission!]!
}

type Organization {
    title: String!
    email: String!
    websiteUrl: String!
    
    seasonList: [Season!]!
    memberList: [User!]!
    ownerList: [User!]!

    members: [UserOrganizationPermit!]
}

enum UserOrganizationPermission {
    OWNER
    MEMBER
}
