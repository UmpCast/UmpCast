extend type Query {
    isRegistered: Boolean
}

extend type Mutation {
    sendSignInLink(
        email: String!
        actionCodeSettings: ActionCodeSettingsInput!
    ): SendSignInLinkPayload!
}

type SendSignInLinkPayload {
    errors: [InputError!]
}