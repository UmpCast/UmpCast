mutation CreateDivision($input: DivisionInput!) {
    createDivision(input: $input) {
        division {
            id
            name
            season {
                id
            }
        }
        errors {
            key
            message
        }
    }
}
