query Screen($gameListingId: ID!) {
    gameListing(id: $gameListingId) {
        id
        assignee {
            node {
                id
            }
        }
        availableAssignees {
            id
            firstName
            lastName
            ...UserAvatar
        }
    }
}
