query AccountScreen {
    viewer {
        id
        ...UseUserAccountEditForm_User
    }
}

mutation UserAccountEdit($input: UpdateUserInput!) {
    updateUser(input: $input) {
        errors {
            key
            message
        }
    }
}
