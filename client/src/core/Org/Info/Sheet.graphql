fragment OrgInfoSheetFragment on UserOrganizationPermit {
    id
    organization {
        id
        email
        websiteUrl
        description
        title
        logoUrl
    }
    permissionLevel
}
