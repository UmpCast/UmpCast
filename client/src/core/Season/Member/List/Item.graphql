fragment SeasonMemberListItem_SeasonParticipantEdge on SeasonParticipantEdge {
    node {
        id
        ...UserItemName_User
        ...UserProfilePicture_User
    }
    permit {
        roles
    }
}
