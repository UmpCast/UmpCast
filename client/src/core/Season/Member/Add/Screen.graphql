query SeasonMemberAddScreen($seasonId: ID!) {
    season(id: $seasonId) {
        id
        memberStatusList {
            added
            permit {
                user {
                    ...SeasonMemberAddItem_User
                }
            }
        }
    }
}
