fragment UseSeasonParticipantOrgRole_Query on Query {
    viewer {
        id
        season(id: $seasonId) {
            permit {
                id
                membership {
                    id
                    role
                }
            }
        }
    }
}
