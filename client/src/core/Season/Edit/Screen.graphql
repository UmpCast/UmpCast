fragment SeasonEditScreen_Season on Season {
    id
    name
    startDate
    endDate
}

query SeasonEditScreen($seasonId: ID!) {
    season(id: $seasonId) {
        ...SeasonEditScreen_Season
    }
}