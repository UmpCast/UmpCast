query UseSeasonViewerOrgRole($seasonId: ID!) {
    viewer {
        id
        season(id: $seasonId) {
            permit {
                id
                membership {
                    id
                    role
                }
            }
        }
    }
}
