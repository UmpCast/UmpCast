query Screen($seasonId: ID!) {
    season(id: $seasonId) {
        id
        name
        participantCount
        organization {
            id
            name
            ...OrgLogo
        }
    }
}
