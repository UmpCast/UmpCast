query Screen($seasonId: ID!) {
    season(id: $seasonId) {
        id
        divisions {
            id
            name
            positions {
                id
                name
            }
        }
    }
}
