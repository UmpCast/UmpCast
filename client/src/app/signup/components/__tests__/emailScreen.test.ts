import { cleanup } from '@testing-library/react-native'

describe('EmailScreen Component', () => {
    beforeEach(cleanup)

    it('Should start with empty fields', () => {})

    it('Should display errors for missinig fields', () => {})

    it('Should display errors for invalid fields', () => {})

    it('Should increment the Signup Step on valid submission', () => {})
})
