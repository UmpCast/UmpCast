mutation SeasonEditAbout($input: UpdateSeasonInput!) {
    updateSeason(input: $input) {
        season {
            id
            endDate
        }
        errors {
            key
            message
        }
    }
}
