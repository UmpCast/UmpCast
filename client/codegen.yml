schema: src/server/schema/*
documents: 
    - src/{features,screens,mutations,hooks,components}/**/*.graphql
generates:
    src/generated.ts:
        plugins:
            - typescript
            - typescript-operations
            - typescript-urql
        config:
            dedupeOperationSuffix: true
            avoidOptionals: true
            scalars: 
                DateTime: string
