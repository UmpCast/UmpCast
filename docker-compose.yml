version: "3.9"

services:
  server:
    build:
      context: server
      dockerfile: Dockerfile
    volumes:
      - ./server:/code 
    ports:
      - "8000:8000"
    command: python manage.py runserver 0.0.0.0:8000
    env_file:
      - .env